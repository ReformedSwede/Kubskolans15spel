/*if(event.equals("scramble")){
			reset();
			Random generator = new Random();
			JButton button1 = new JButton();
			JButton button2 = new JButton();
			
			for(int i = 0; i < 6*side*side; i++){	//Limit must be even!!
				button1 = grid[generator.nextInt(side)][generator.nextInt(side)];
				do{
					button2 = grid[generator.nextInt(side)][generator.nextInt(side)];
				}while((button1.getText().equals("") || button2.getText().equals("")) && !isNextTo(button1, button2));	//Only move the disabled button one step horizontally or vertically
								
				if(button1.getText().equals("") || button2.getText().equals("")){ //make one extra swap if we swapped the disabled button
					i--;
				}
				
				if(button1 != button2){	//only swap if not the same button
					swapButtons(button1, button2);
				}
				else
					i--;
			}*/